<Window x:Class="VoiceDictation.ReplacementsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Text Replacements"
        Width="450" Height="400"
        MinWidth="350" MinHeight="300"
        Background="#1E1E2E"
        FontFamily="Segoe UI"
        ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="REPLACEMENT RULES"
                   Foreground="#BAC2DE" FontSize="11" FontWeight="SemiBold"
                   Margin="0,0,0,8"/>

        <!-- DataGrid -->
        <DataGrid Grid.Row="1" x:Name="RulesGrid"
                  Background="#181825" Foreground="#CDD6F4"
                  BorderBrush="#45475A" BorderThickness="1"
                  GridLinesVisibility="None"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionMode="Single"
                  HeadersVisibility="Column"
                  RowBackground="#181825"
                  AlternatingRowBackground="#181825"
                  Margin="0,0,0,8">

            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#313244"/>
                    <Setter Property="Foreground" Value="#BAC2DE"/>
                    <Setter Property="Padding" Value="8,4"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="#CDD6F4"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Padding" Value="4,2"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#313244"/>
                            <Setter Property="Foreground" Value="#CDD6F4"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.CellStyle>

            <DataGrid.Columns>
                <DataGridTextColumn Header="Trigger" Binding="{Binding Trigger, UpdateSourceTrigger=PropertyChanged}" Width="*">
                    <DataGridTextColumn.EditingElementStyle>
                        <Style TargetType="TextBox">
                            <Setter Property="Background" Value="#313244"/>
                            <Setter Property="Foreground" Value="#CDD6F4"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="4,2"/>
                        </Style>
                    </DataGridTextColumn.EditingElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Replacement" Binding="{Binding Replacement, UpdateSourceTrigger=PropertyChanged}" Width="*">
                    <DataGridTextColumn.EditingElementStyle>
                        <Style TargetType="TextBox">
                            <Setter Property="Background" Value="#313244"/>
                            <Setter Property="Foreground" Value="#CDD6F4"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="4,2"/>
                        </Style>
                    </DataGridTextColumn.EditingElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Button Bar -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Open File" Click="OpenFile_Click"
                    Background="Transparent" Foreground="#89B4FA"
                    BorderThickness="0" Padding="12,6" Margin="0,0,8,0"
                    Cursor="Hand"/>
            <Button Content="Add" Click="Add_Click"
                    Background="#89B4FA" Foreground="#1E1E2E"
                    FontWeight="SemiBold"
                    BorderThickness="0" Padding="12,6" Margin="0,0,8,0"
                    Cursor="Hand"/>
            <Button Content="Remove" Click="Remove_Click"
                    Background="#F38BA8" Foreground="#1E1E2E"
                    FontWeight="SemiBold"
                    BorderThickness="0" Padding="12,6"
                    Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
