<Window x:Class="PureType.StatsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Statistics"
        Width="420" Height="480"
        MinWidth="350" MinHeight="350"
        ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}"
        FontFamily="Segoe UI">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Summary Cards -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Today -->
            <Border Grid.Column="0" Background="{DynamicResource SurfaceBrush}"
                    CornerRadius="8" Padding="16,12"
                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="TODAY" Foreground="{DynamicResource AccentBrush}"
                               FontSize="11" FontWeight="Bold" Margin="0,0,0,8"/>
                    <TextBlock x:Name="TodayWords" Foreground="{DynamicResource TextBrush}"
                               FontSize="24" FontWeight="Bold"/>
                    <TextBlock Text="words" Foreground="{DynamicResource TextDimBrush}" FontSize="11"/>
                    <TextBlock x:Name="TodaySessions" Foreground="{DynamicResource TextBrush}"
                               FontSize="14" Margin="0,8,0,0"/>
                    <TextBlock x:Name="TodayDuration" Foreground="{DynamicResource TextBrush}"
                               FontSize="14"/>
                </StackPanel>
            </Border>

            <!-- All Time -->
            <Border Grid.Column="2" Background="{DynamicResource SurfaceBrush}"
                    CornerRadius="8" Padding="16,12"
                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="ALL TIME" Foreground="{DynamicResource AccentBrush}"
                               FontSize="11" FontWeight="Bold" Margin="0,0,0,8"/>
                    <TextBlock x:Name="TotalWords" Foreground="{DynamicResource TextBrush}"
                               FontSize="24" FontWeight="Bold"/>
                    <TextBlock Text="words" Foreground="{DynamicResource TextDimBrush}" FontSize="11"/>
                    <TextBlock x:Name="TotalSessions" Foreground="{DynamicResource TextBrush}"
                               FontSize="14" Margin="0,8,0,0"/>
                    <TextBlock x:Name="TotalDuration" Foreground="{DynamicResource TextBrush}"
                               FontSize="14"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- History Header -->
        <TextBlock Grid.Row="1" Text="LAST 30 DAYS" Foreground="{DynamicResource AccentBrush}"
                   FontSize="11" FontWeight="Bold" Margin="0,0,0,8"/>

        <!-- History Table -->
        <DataGrid Grid.Row="2" x:Name="HistoryGrid"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  BorderThickness="1"
                  BorderBrush="{DynamicResource BorderBrush}"
                  Background="{DynamicResource SurfaceBrush}"
                  Foreground="{DynamicResource TextBrush}"
                  RowBackground="{DynamicResource SurfaceBrush}"
                  AlternatingRowBackground="{DynamicResource DeepBrush}"
                  FontSize="12"
                  HorizontalGridLinesBrush="{DynamicResource BorderBrush}">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="{DynamicResource DeepBrush}"/>
                    <Setter Property="Foreground" Value="{DynamicResource TextDimBrush}"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="FontSize" Value="11"/>
                    <Setter Property="Padding" Value="8,6"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="*"/>
                <DataGridTextColumn Header="Words" Binding="{Binding Words}" Width="70"/>
                <DataGridTextColumn Header="Sessions" Binding="{Binding Sessions}" Width="70"/>
                <DataGridTextColumn Header="Duration" Binding="{Binding DurationDisplay}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
