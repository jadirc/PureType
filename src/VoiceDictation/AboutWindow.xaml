<Window x:Class="VoiceDictation.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About Voice Dictation"
        Width="380" Height="480"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource BackgroundBrush}"
        FontFamily="Segoe UI">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- App Name + Version -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,12">
            <TextBlock Text="Voice Dictation" Foreground="{DynamicResource TextBrush}"
                       FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"/>
            <TextBlock x:Name="VersionText" Text="Version 1.0.0" Foreground="{DynamicResource TextDimBrush}"
                       FontSize="12" HorizontalAlignment="Center" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- GitHub Link -->
        <TextBlock Grid.Row="1" HorizontalAlignment="Center" Margin="0,0,0,16">
            <Hyperlink x:Name="GithubLink" NavigateUri="https://github.com/jadirc/VoiceDictation"
                       RequestNavigate="Hyperlink_RequestNavigate"
                       Foreground="{DynamicResource AccentBrush}">
                github.com/jadirc/VoiceDictation
            </Hyperlink>
        </TextBlock>

        <!-- Open Source Header -->
        <TextBlock Grid.Row="2" Text="OPEN SOURCE LIBRARIES" Foreground="{DynamicResource AccentBrush}"
                   FontSize="12" FontWeight="Bold" Margin="0,0,0,8"/>

        <!-- Libraries List -->
        <Border Grid.Row="3" Background="{DynamicResource DeepBrush}" CornerRadius="6" Padding="12">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="LibrariesPanel">
                    <!-- Populated in code-behind -->
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Update Check -->
        <StackPanel Grid.Row="4" HorizontalAlignment="Center" Margin="0,10,0,0" Orientation="Horizontal">
            <Button x:Name="UpdateButton" Content="Check for Updates"
                    Click="UpdateButton_Click"
                    Background="Transparent" Foreground="{DynamicResource AccentBrush}"
                    FontSize="12" BorderThickness="0" Padding="8,4" Cursor="Hand"/>
            <TextBlock x:Name="UpdateStatus" Foreground="{DynamicResource TextDimBrush}"
                       FontSize="12" VerticalAlignment="Center" Margin="8,0,0,0"/>
        </StackPanel>

        <!-- Close -->
        <Button Grid.Row="5" Content="Close" IsCancel="True"
                Background="{DynamicResource AccentBrush}" Foreground="{DynamicResource AccentForegroundBrush}"
                FontWeight="SemiBold" FontSize="13"
                BorderThickness="0" Padding="24,8"
                HorizontalAlignment="Center" Margin="0,12,0,0"
                Cursor="Hand"/>
    </Grid>
</Window>
