<Window x:Class="VoiceDictation.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About Voice Dictation"
        Width="380" Height="420"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="#1E1E2E"
        FontFamily="Segoe UI">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- App Name + Version -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,12">
            <TextBlock Text="Voice Dictation" Foreground="#CDD6F4"
                       FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"/>
            <TextBlock x:Name="VersionText" Text="Version 1.0.0" Foreground="#6C7086"
                       FontSize="12" HorizontalAlignment="Center" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- GitHub Link -->
        <TextBlock Grid.Row="1" HorizontalAlignment="Center" Margin="0,0,0,16">
            <Hyperlink x:Name="GithubLink" NavigateUri="https://github.com/jadirc/VoiceDictation"
                       RequestNavigate="Hyperlink_RequestNavigate"
                       Foreground="#89B4FA">
                github.com/jadirc/VoiceDictation
            </Hyperlink>
        </TextBlock>

        <!-- Open Source Header -->
        <TextBlock Grid.Row="2" Text="OPEN SOURCE LIBRARIES" Foreground="#89B4FA"
                   FontSize="12" FontWeight="Bold" Margin="0,0,0,8"/>

        <!-- Libraries List -->
        <Border Grid.Row="3" Background="#181825" CornerRadius="6" Padding="12">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="LibrariesPanel">
                    <!-- Populated in code-behind -->
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Close -->
        <Button Grid.Row="4" Content="Close" IsCancel="True"
                Background="#89B4FA" Foreground="#1E1E2E"
                FontWeight="SemiBold" FontSize="13"
                BorderThickness="0" Padding="24,8"
                HorizontalAlignment="Center" Margin="0,12,0,0"
                Cursor="Hand"/>
    </Grid>
</Window>
